{
  "name": "plat-telegram-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply plat_telegram --local",
    "db:migrate:remote": "wrangler d1 migrations apply plat_telegram --remote",
    "db:seed-fts:local": "node scripts/seed-fts-index.mjs",
    "db:seed-fts:remote": "node scripts/seed-fts-index.mjs --remote",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
